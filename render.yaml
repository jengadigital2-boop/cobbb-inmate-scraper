services:
  - type: web
    name: cobb-inmate-scraper
    env: docker          # KEY CHANGE: Docker instead of node runtime
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: AUTH_TOKEN
        generateValue: true   # Render auto-generates a random secret — copy it after deploy
      - key: PORT
        value: 3000
